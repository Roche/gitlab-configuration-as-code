sudo: false
language: python
python:
  - "3.6"
  - "3.7"
  - "3.8"

git:
  depth: false

stages:
  - lint
  - test

install: pip install tox-travis
script: tox
jobs:
  include:
    - stage: lint
      script: make lint
    - stage: test
      script: tox
